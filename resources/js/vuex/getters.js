export const users = state => {
    return state.users.model
}

export const title = state => {
    return state.title.data
}

export const country = state => {
    return state.country.model
}

export const profile = state => {
    return state.profile.data
}
